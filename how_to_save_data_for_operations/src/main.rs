mod data;
use data::{primitive};



fn main() {
    // derived::user_defined::run();

    primitive::scalar::run( );
    primitive::scalar::matthew();
    primitive::scalar::osamor();
    primitive::scalar::mommy();
    primitive::scalar::analyze_slice(&[1, 2, 3,4]);
    // primitive::compound::
    println!("");
    println!("Hello, world!");
}
